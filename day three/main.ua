# Experimental!
Dig ← "(\\d{1,3})"
Mul ← $"mul\\(_\\,_\\)" Dig Dig

# Part 1
&fras "data.txt"
regex Mul
/+≡(/×⋕↘1)

# Part 2
&fras "data.txt"
⊜□ ¬⊸⦷"do()"
⍚(⍣(⊢|∘) ⊚⊸⌕"don't()")
/◇⊂⍚(↙)

regex Mul
/+≡(/×⋕↘1)
